# Schematic status

Sheet-by-sheet status of the Altium hierarchical schematic. Edit in place as sheets progress; log the *milestone* (sheet started/completed) in CHANGELOG.md.

Net naming convention: match Radxa naming where possible for cross-reference during debug. Hierarchical ports required on every net that crosses sheet boundaries — Radxa uses flat topology with inconsistent naming, which requires careful translation into Altium's hierarchical port system. Key nets: `VSYS`, `VDD_CPU`, `VDD_GPU`, `VDD_NPU`, `VDD_LOGIC`, `VCC_DDR`, `VCC_3V3`, `VCC_1V8`, `I2C_SCL`, `I2C_SDA`.

**Row numbering below now matches `cradle_sidecar/data/altium/sheet-map.md`'s real 9-sheet structure** (previously this table had a stale 10-row split that didn't match the actual Altium sheets — e.g. Interfaces was split into two rows, and a "System" row existed that doesn't correspond to any sheet in `sheet-map.md`; see note at the bottom).

| # | Sheet | Status | Notes |
|---|---|---|---|
| 1 | Cradle (top-level) | Not tracked here | Block diagram only, no discrete parts to mark complete |
| 2 | Power — Charging | **Complete** | BQ25601, battery connector, USB1 CC resistors, VSYS output, TVS, reverse polarity protection, NTC thermistor, QON switch. Rechecked 2026-07-21: 2 unexplained orphan pins (`SW1-5`, `U10-8`/`U10-10`) — no BQ25601/switch datasheet on file to confirm these are legitimate NC pins vs. a real gap; likely benign (mechanical/reserved pins are common on switches and charge ICs) but not independently verified |
| 3 | Power — PMIC | **Complete** | RK806S-5, all 10 buck regulators (inductors + decoupling) done. **Rechecked 2026-07-21, corrected same day**: initially flagged this row as still incomplete based on 5 orphaned pins, but that undersold the real state — every item this row's "Remaining" note used to list is actually done and reaching the top sheet through named hierarchical ports (`PMIC_PWR_CTRL_1`/`2` and `PMIC_EXT_EN_OUT` for enable/PWRON-class control, `RESET_L` for reset, `I2C1_SCL_M0_RK806`/`I2C1_SDA_M0_RK806` for the I2C interface, `PMIC_INT_L` for fault/interrupt), and all 5 `NLDO1`-`5` plus 5 `PLDO1`-`5` outputs have their decoupling caps connected (`C42`-`C46`, `C59`-`C63`). Same "named single-pin net, pending Compute-SoC" pattern as every other complete sheet on this board. 5 pins remain genuinely unlabeled/unexplained (`U6-2`, `U6-3`, `U6-21`, `U6-38`, `U6-48`, no RK806S-5 datasheet on file to identify them) — a minor loose end, same tier as the small unexplained orphans already accepted on the Power-Charging and Interfaces rows, not a reason to withhold Complete |
| 4 | Compute — SoC | Not started | RK3576 pin assignments, power pins to named nets, clock, reset, boot mode resistors, JTAG pads. **Do this sheet last** — by then every net it connects to already has a name |
| 5 | Compute — Memory | Not started | LPDDR4X (K4UBE3D4AB-MGCL, U5), all decoupling. eMMC lives on Storage (row 7) — real BOM/netlist has `U3`/EMMC04G-M627-Y02U on `Cradle_Storage`, not here |
| 6 | Wireless | **Complete** | AP6275S: SDIO (4-bit) to SoC, UART to SoC (BT HCI; PCM omitted), 2x Johanson 2450AT18A100E 2.4GHz chip antennas + matching networks, 37.4MHz crystal + load caps, 32.768kHz SiTime MEMS oscillator on LPO. 2.4GHz-only (5GHz dropped). **Rechecked 2026-07-21**: `cradle_sidecar/data/components/AP6275S.md` was stale (several pins marked `Open`/`UNVERIFIED` from a 2026-07-08 export) — reconfirmed against the current export and updated; `wiring_check.py` now shows 0 open items. All "orphaned" `U2` pins turned out to be pins the card already documents as intentional NC/not-routed (12, 24, 37, 44-48 — PCM interface unused, family-variant-only pins) |
| 7 | Storage and SD | **Complete** | MicroSD slot (TF-01A, Card1): SD-mode wiring, CMD/DAT0-3 + mechanical card-detect pull-ups (30kOhm to STORAGE_3V3) done. eMMC (EMMC04G-M627-Y02U, U3): full 153-ball pin table, HS400 targeting, all signals wired (CMD/DAT0-7/RST_n/CLK/DS/VCC/VCCQ/VDDi), DS pull-down FET (`Q2`) built. **Rechecked 2026-07-21**: both `TF-01A.md` and `EMMC04G-M627-Y02U.md` show 0 open items via `wiring_check.py` |
| 8 | Interfaces | **Complete** | HDMI (`HDMI-519S.md`, U8): full pinout, level-shifting (CEC/SCL/SDA/HPD), ESD protection on all 4 TMDS pairs, `+5V` current-limiting still open. FPC1 (`FPC1.md`, GPIO/expansion header, 30P): 10 GPIO + I2C + SPI + UART, ESD on I2C/UART, hard constraint documented requiring a 3.3V-capable RK3576 bank (no level-shifter). FPC2 (`FPC2.md`, MIPI CSI camera, 22P `05A20L22P`): standalone `CSI0` 4-lane interface, PWDN level-shift circuit built, ESD on every signal. **`U4`/`AP2141WG-7` (load switch) deleted by the user in local Altium, confirmed in the 2026-07-21 23:42 re-export** (component count dropped 262→261, no `U4`/`AP2141WG-7` row in the BOM). No card ever referenced it, so no card cleanup was needed. Two unexplained orphan pins remain on `J1` (`J1-5`/`J1-6`, a small header, minor/likely benign) |
| 9 | Audio | **Complete** | MAX98357AEWL+T (U9): mono speaker, I2S Channel 0 (SD_MODE HIGH), GAIN NC (9dB), OUTP/OUTN to speaker JST-PH on Interfaces sheet. Both MEMS mics (MIC1 right/SELECT-GND, MIC2 left/SELECT-3V3) share CLK/DATA, split by rising/falling clock edge. RK3576's 2x PDM controllers + 5x SAI/I2S blocks handle both natively — no external codec. **Rechecked 2026-07-21**: zero orphaned pins found for U9/MIC1/MIC2 in the current export — good sign, though no component card exists for this sheet's parts, so this is a netlist-only check, not a card-verified one like rows 6-8 |

**Note on the old row 10 ("System")**: the previous version of this table had a 10th row for "Debug UART header, power/reset buttons, status LEDs, crystal oscillator, boot mode resistors" that doesn't correspond to any sheet name in `cradle_sidecar/data/altium/sheet-map.md`'s real 9-sheet list. Some of that scope may already live on other sheets (e.g. the power button is `SW1` on Power — Charging) or may belong on Compute — SoC once that sheet is built (crystal/boot-mode resistors are typically SoC-adjacent). Left out of the table above rather than guessed at — needs a decision on whether "System" is a real, separate Altium sheet that still needs creating, or whether its scope has been absorbed elsewhere.
